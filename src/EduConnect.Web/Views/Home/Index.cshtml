@model EduConnect.Web.ViewModels.DashboardViewModel
@{
    ViewData["Title"] = "Dashboard";
    var roleName = Context.Session.GetString("RoleName");
    var userName = Context.Session.GetString("UserName");
}

<div class="ec-hero mb-4">
    <div style="position:relative;z-index:1">
        <div class="ec-hero-sub small mb-1">@DateTime.Now.ToString("dddd, MMMM d, yyyy")</div>
        <h3 class="fw-bold mb-1">Welcome back, @(Context.Session.GetString("UserName") ?? "Guest") 👋</h3>
        <div class="ec-hero-sub">Here's what's happening on campus today.</div>
    </div>
</div>

<!-- ─── STAT CARDS ─────────────────────── -->
<div class="row g-3 mb-4">

    <!-- Total Announcements -->
    <div class="col-6 col-md-3">
        <div class="card border-0 shadow-sm h-100 ec-lift">
            <div class="card-body d-flex align-items-center gap-3">
                <div class="ec-stat-icon bg-primary bg-opacity-10">
                    <i class="bi bi-megaphone text-primary fs-4"></i>
                </div>
                <div>
                    <div class="fw-bold fs-4">
                        @Model.TotalAnnouncements
                    </div>
                    <div class="text-muted small">
                        Total Announcements
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- Today's Announcements -->
    <div class="col-6 col-md-3">
        <div class="card border-0 shadow-sm h-100 ec-lift">
            <div class="card-body d-flex align-items-center gap-3">
                <div class="ec-stat-icon bg-success bg-opacity-10">
                    <i class="bi bi-calendar-check text-success fs-4"></i>
                </div>
                <div>
                    <div class="fw-bold fs-4">
                        @Model.TodayAnnouncements
                    </div>
                    <div class="text-muted small">
                        Posted Today
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- Unread Notifications -->
    <div class="col-6 col-md-3">
        <div class="card border-0 shadow-sm h-100 ec-lift">
            <div class="card-body d-flex align-items-center gap-3">
                <div class="ec-stat-icon bg-warning bg-opacity-10">
                    <i class="bi bi-bell text-warning fs-4"></i>
                </div>
                <div>
                    <div class="fw-bold fs-4">
                        @Model.UnreadNotifications
                    </div>
                    <div class="text-muted small">
                        Unread Notifications
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- Role Specific Card -->
    @if (roleName == "Administrator")
    {
        <div class="col-6 col-md-3">
            <div class="card border-0 shadow-sm h-100 ec-lift">
                <div class="card-body d-flex align-items-center gap-3">
                    <div class="ec-stat-icon bg-info bg-opacity-10">
                        <i class="bi bi-people text-info fs-4"></i>
                    </div>
                    <div>
                        <div class="fw-bold fs-4">
                            @Model.TotalUsers
                        </div>
                        <div class="text-muted small">
                            Active Users
                        </div>
                    </div>
                </div>
            </div>
        </div>
    }
    else if (roleName == "Faculty" || roleName == "Staff")
    {
        <div class="col-6 col-md-3">
            <div class="card border-0 shadow-sm h-100 ec-lift">
                <div class="card-body d-flex align-items-center gap-3">
                    <div class="ec-stat-icon bg-info bg-opacity-10">
                        <i class="bi bi-eye text-info fs-4"></i>
                    </div>
                    <div>
                        <div class="fw-bold fs-4">
                            @Model.TotalViews
                        </div>
                        <div class="text-muted small">
                            Total Views
                        </div>
                    </div>
                </div>
            </div>
        </div>
    }
    else
    {
        <div class="col-6 col-md-3">
            <div class="card border-0 shadow-sm h-100 ec-lift">
                <div class="card-body d-flex align-items-center gap-3">
                    <div class="ec-stat-icon bg-info bg-opacity-10">
                        <i class="bi bi-calendar-event text-info fs-4"></i>
                    </div>
                    <div>
                        <div class="fw-bold fs-4">
                            @Model.UpcomingEvents
                        </div>
                        <div class="text-muted small">
                            Upcoming Events
                        </div>
                    </div>
                </div>
            </div>
        </div>
    }

</div>

<!-- ─── UPCOMING EVENTS ───────────────── -->
<div class="row g-3 mb-4">
    <div class="col-12">
        <div class="card border-0 shadow-sm">
            <div class="card-header bg-white border-0 pt-3 pb-2 d-flex justify-content-between align-items-center">
                <h6 class="fw-bold mb-0">
                    <i class="bi bi-calendar-event me-2 text-primary"></i>
                    Upcoming Events
                </h6>
                <a href="/Event" class="btn btn-sm btn-outline-primary">See all</a>
            </div>
            <div class="card-body p-0">
                @if (Model.UpcomingEventsList.Count == 0)
                {
                    <div class="ec-empty">
                        <i class="bi bi-calendar-x"></i>
                        No upcoming events right now
                    </div>
                }
                else
                {
                    <ul class="list-group list-group-flush">
                        @foreach (var ev in Model.UpcomingEventsList)
                        {
                            <li class="list-group-item d-flex align-items-center gap-3 py-3">
                                <div class="text-center flex-shrink-0" style="min-width:48px;">
                                    <div class="fw-bold text-primary lh-1 fs-5">@ev.StartDateTime.ToString("dd")</div>
                                    <div class="text-muted small text-uppercase">@ev.StartDateTime.ToString("MMM")</div>
                                </div>
                                <div class="flex-grow-1 overflow-hidden">
                                    <div class="fw-semibold text-truncate">@ev.EventTitle</div>
                                    <small class="text-muted">
                                        @if (ev.IsOnline)
                                        {
                                            <i class="bi bi-camera-video me-1"></i>@:Online
                                        }
                                        else if (!string.IsNullOrEmpty(ev.Location))
                                        {
                                            <i class="bi bi-geo-alt me-1"></i>@ev.Location
                                        }
                                    </small>
                                </div>
                                <a href="/Event/Details/@ev.EventID"
                                   class="btn btn-sm btn-outline-primary flex-shrink-0">
                                    View
                                </a>
                            </li>
                        }
                    </ul>
                }
            </div>
        </div>
    </div>
</div>

@{
    bool isStudentFeed = roleName == "Student";
}

@if (isStudentFeed)
{
    <!-- ─── STUDENT SEARCH BAR ─────────────── -->
    <div class="card border-0 shadow-sm mb-3">
        <div class="card-body py-2">
            <form method="get" class="d-flex gap-2 flex-wrap align-items-center">
                <input type="text"
                       name="searchQuery"
                       value="@Model.SearchQuery"
                       class="form-control form-control-sm"
                       placeholder="Search announcements..."
                       style="width: 220px;" />
                <select name="filterFeedType"
                        class="form-select form-select-sm"
                        style="width: 150px;">
                    <option value="">All Feeds</option>
                    <option value="Academic"
                            selected="@(Model.FilterFeedType == "Academic")">
                        Academic
                    </option>
                    <option value="NonAcademic"
                            selected="@(Model.FilterFeedType == "NonAcademic")">
                        Non-Academic
                    </option>
                </select>
                <button type="submit" class="btn btn-primary btn-sm">
                    <i class="bi bi-search"></i> Search
                </button>
                @if (!string.IsNullOrEmpty(Model.SearchQuery) ||
                     !string.IsNullOrEmpty(Model.FilterFeedType))
                {
                    <a href="/" class="btn btn-outline-secondary btn-sm">
                        <i class="bi bi-x"></i> Clear
                    </a>
                }
            </form>
        </div>
    </div>

    <!-- ─── SECTION 1: YOUR DEPARTMENT ───── -->
    @if (Model.DepartmentAnnouncements.Count > 0)
    {
        <div class="card border-0 shadow-sm mb-3">
            <div class="card-header bg-white border-0 pt-3 pb-2">
                <h6 class="fw-bold mb-0">
                    <i class="bi bi-pin-angle-fill me-2 text-primary"></i>
                    Your Department
                    <span class="text-muted fw-normal small ms-1">— guaranteed for you</span>
                </h6>
            </div>
            <div class="card-body p-0">
                <div class="table-responsive">
                    @await Html.PartialAsync("_AnnouncementRows", Model.DepartmentAnnouncements)
                </div>
            </div>
        </div>
    }

    <!-- ─── SECTION 2: FOR YOU ────────────── -->
    <div class="card border-0 shadow-sm mb-3">
        <div class="card-header bg-white border-0 pt-3 pb-2">
            <h6 class="fw-bold mb-0">
                <i class="bi bi-stars me-2 text-warning"></i>
                For You
                <span class="text-muted fw-normal small ms-1">— based on what you read</span>
            </h6>
        </div>
        <div class="card-body p-0">
            @if (Model.ForYouAnnouncements.Count == 0)
            {
                <div class="ec-empty">
                    <i class="bi bi-binoculars"></i>
                    No personalized picks yet — browse more announcements to train your feed
                </div>
            }
            else
            {
                <div class="table-responsive">
                    @await Html.PartialAsync("_AnnouncementRows", Model.ForYouAnnouncements)
                </div>
            }
        </div>
    </div>

    <!-- ─── SECTION 3: EXPLORE ────────────── -->
    @if (Model.ExploreAnnouncements.Count > 0)
    {
        <div class="card border-0 shadow-sm mb-3">
            <div class="card-header bg-white border-0 pt-3 pb-2">
                <h6 class="fw-bold mb-0">
                    <i class="bi bi-compass me-2 text-success"></i>
                    Explore
                    <span class="text-muted fw-normal small ms-1">— from other departments</span>
                </h6>
            </div>
            <div class="card-body p-0">
                <div class="table-responsive">
                    @await Html.PartialAsync("_AnnouncementRows", Model.ExploreAnnouncements)
                </div>
            </div>
        </div>
    }
}
else
{
    <!-- ─── SEARCH + TABLE (non-student roles) ─── -->
    <div class="card border-0 shadow-sm">
        <div class="card-header bg-white border-0 pt-3">
            <div class="d-flex justify-content-between
                        align-items-center flex-wrap gap-2">
                <h6 class="fw-bold mb-0">
                    <i class="bi bi-table me-2 text-primary"></i>
                    Recent Announcements
                </h6>

                <form method="get" class="d-flex gap-2 flex-wrap">
                    <input type="text"
                           name="searchQuery"
                           value="@Model.SearchQuery"
                           class="form-control form-control-sm"
                           placeholder="Search announcements..."
                           style="width: 200px;" />

                    <select name="filterFeedType"
                            class="form-select form-select-sm"
                            style="width: 140px;">
                        <option value="">All Feeds</option>
                        <option value="Academic"
                                selected="@(Model.FilterFeedType == "Academic")">
                            Academic
                        </option>
                        <option value="NonAcademic"
                                selected="@(Model.FilterFeedType == "NonAcademic")">
                            Non-Academic
                        </option>
                    </select>

                    <button type="submit" class="btn btn-primary btn-sm">
                        <i class="bi bi-search"></i>
                        Search
                    </button>

                    @if (!string.IsNullOrEmpty(Model.SearchQuery) ||
                         !string.IsNullOrEmpty(Model.FilterFeedType))
                    {
                        <a href="/" class="btn btn-outline-secondary btn-sm">
                            <i class="bi bi-x"></i>
                            Clear
                        </a>
                    }
                </form>
            </div>
        </div>

        <div class="card-body p-0">
            <div class="table-responsive">
                <table class="table table-hover mb-0">
                    <thead class="table-light">
                        <tr>
                            <th class="ps-3">Title</th>
                            <th>Category</th>
                            <th>Feed</th>
                            <th>Tags</th>
                            <th>Author</th>
                            <th>Views</th>
                            <th>Date</th>
                            <th>Status</th>
                        </tr>
                    </thead>
                    <tbody>
                        @if (Model.RecentAnnouncements.Count == 0)
                        {
                            <tr>
                                <td colspan="8">
                                    <div class="ec-empty">
                                        <i class="bi bi-inbox"></i>
                                        No announcements found
                                    </div>
                                </td>
                            </tr>
                        }
                        else
                        {
                            @foreach (var item in Model.RecentAnnouncements)
                            {
                                <tr>
                                    <td class="ps-3">
                                        <a href="/Announcement/Details/@item.AnnouncementID"
                                           class="text-decoration-none fw-semibold text-dark">
                                            @item.Title
                                        </a>
                                    </td>
                                    <td>
                                        <span class="badge rounded-pill"
                                              style="background-color: @item.CategoryColor">
                                            @item.CategoryName
                                        </span>
                                    </td>
                                    <td>
                                        @if (item.FeedType == "Academic")
                                        {
                                            <span class="badge bg-primary">
                                                <i class="bi bi-mortarboard me-1"></i>Academic
                                            </span>
                                        }
                                        else if (item.FeedType == "Emergency")
                                        {
                                            <span class="badge bg-danger">
                                                <i class="bi bi-exclamation-triangle me-1"></i>Emergency
                                            </span>
                                        }
                                        else
                                        {
                                            <span class="badge bg-secondary">
                                                <i class="bi bi-building me-1"></i>Non-Academic
                                            </span>
                                        }
                                    </td>
                                    <td>
                                        @foreach (var tag in item.Tags.Take(2))
                                        {
                                            <span class="badge bg-light text-dark border me-1">@tag</span>
                                        }
                                        @if (item.Tags.Count > 2)
                                        {
                                            <span class="badge bg-light text-muted border">
                                                +@(item.Tags.Count - 2)
                                            </span>
                                        }
                                    </td>
                                    <td class="text-muted small">@item.AuthorName</td>
                                    <td class="text-muted small">
                                        <i class="bi bi-eye me-1"></i>@item.ViewCount
                                    </td>
                                    <td class="text-muted small">
                                        @(item.PublishedAt.HasValue
                                            ? item.PublishedAt.Value.ToString("MMM dd, yyyy")
                                            : "—")
                                    </td>
                                    <td>
                                        @if (item.Status == "Published")
                                        {
                                            <span class="badge bg-success">Published</span>
                                        }
                                        else if (item.Status == "Draft")
                                        {
                                            <span class="badge bg-warning text-dark">Draft</span>
                                        }
                                        else
                                        {
                                            <span class="badge bg-secondary">@item.Status</span>
                                        }
                                    </td>
                                </tr>
                            }
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
}

